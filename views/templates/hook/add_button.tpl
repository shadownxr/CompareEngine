<div>
    <form action="{$url}" method="POST">
        <input type="hidden" name="product-id" value="{$product.id}">
        <input type="hidden" name="action" value="add">
        <button class="btn btn-primary" type="submit"> Add to compare </button>
    </form>
</div>